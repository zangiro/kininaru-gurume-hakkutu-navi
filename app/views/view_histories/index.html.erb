<% breadcrumb :view_history_index, current_user %>

<div class="container">
<div class="row">
<h3>ｋじ</h3>
<% @a2.each do |post| %>
  <div class="bg-info col-2">
  <a href="<%= post_path(post) %>">
  
  <% if post.main_image.attached? %>
    <%= image_tag post.main_image.variant(resize_and_pad: [200, 200]) %>
  <% end %>
  </a>
  <div class="row">
  <%= post.dish.introduction %>
  </div>
  </div>
<% end %>
</div>
</div>
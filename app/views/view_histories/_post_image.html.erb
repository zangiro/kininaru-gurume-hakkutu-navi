<a href="<%= post_path(post, user_id: @user.id, post_path: @post_path) %>">
  <% if post.main_image.attached? %>
    <%= image_tag post.decorated.resized_main_image_small %>
  <% end %>
</a>

<div class="mt-1">
  <%= truncate(post.title, length: MAXIMUM_POST_TITLE, omission: '...') %>
  <%# ーーーーーーtruncateはRailsのヘルパーメソッドで文字列を指定した長さで切り詰める機能がある。lengthで文字数を指定ーーーーーーーーーーー %>
  <%# ーーーーーーomissionは、切り詰めた際に追加する文字列を指定するためのオプション。ーーーーーーーーーーーー %>
</div>
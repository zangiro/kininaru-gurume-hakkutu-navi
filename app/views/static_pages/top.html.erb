<div class="container">
  <div class="row">
    <div class="d-flex">
      <% unless logged_in? %>
        <%= link_to '@新規作成', new_user_path %>
        <%= link_to '@ログイン', login_path %>
      <% end %>
    </div>
  </div>

  <%# ----------------------------ロゴの行----------------------------- %>

  <div class="d-flex justify-content-around mt-3">

    <div class="row">
      <% if @posts[1].present? %>
        <%= render "post_image", posts: @posts, number: 0 %>
      <% end %>
    </div>

    <div class="row bg-danger w-25 mt-5">
      <div class="d-flex align-items-center display-6">
        気になるグルメ発掘ナビ
      </div>
    </div>

    <div class="row">
      <%= render "post_image", posts: @posts, number: 1 %>
    </div>

  </div>

  <%# ---------------------------アプリ説明の行------------------------------ %>

  <div class="d-flex justify-content-around mt-3">

    <div class="row">
      <% if @posts[3].present? %>
        <%= render "post_image", posts: @posts, number: 2 %>
      <% end %>
    </div>

    <div class="row bg-info w-50 mt-5" style="height: 90px;">
      <div class="d-flex align-items-center">
        @このアプリは簡単操作で自分の知らないご当地グルメ、郷土料理発見を目的とするアプリです。
      </div>
    </div>

    <div class="row">
      <%= render "post_image", posts: @posts, number: 3 %>
    </div>
  </div>

  <%# ----------------------------診断のボタンの行----------------------------- %>

  <div class="d-flex justify-content-around mt-3">

    <div class="row">
      <% if @posts[5].present? %>
        <%= render "post_image", posts: @posts, number: 4 %>
      <% end %>
    </div>

    <div class="row mt-5">
        <%= link_to '@診断スタート', tags_path, class: 'd-flex align-items-center justify-content-center bg-danger display-6', style: 'height: 80px; text-decoration: none; color: white;' %>
    </div>

    <div class="row">
      <%= render "post_image", posts: @posts, number: 5 %>
    </div>
  </div>

  <%# ----------------------------アプリの使用説明の行----------------------------- %>
  
  <div class="d-flex justify-content-around mt-3">

    <div class="row">
      <% if @posts[7].present? %>
        <%= render "post_image", posts: @posts, number: 6 %>
      <% end %>
    </div>

    <div class="row mt-5 bg-info" style="height: 150px;">
      [操作説明]<br>
      １、診断スタートボタンを押す。<br>
      ２、興味のあるジャンル、地域などを選ぶ。<br>
      ３、関連するグルメ情報（画像）が出る。<br>
      ５、クリックで詳細が表示される。<br>
      ６、現在地を入力すると、その料理が食べられる一番近くのお店への経路情報が出る。（未実装）
    </div>

    <div class="row">
      <%= render "post_image", posts: @posts, number: 7 %>
    </div>
  </div>


</div>







<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="bg-info w-75" style="height: 400px;">
      紹介絵
    </div>
  </div>
</div>

<div class="container mt-3">
  <div class="row justify-content-center">
  <%= link_to '@診断スタート', tags_path, class: 'd-flex align-items-center justify-content-center w-25 bg-danger text-center display-6', style: 'height: 80px; text-decoration: none; color: white;' %>
  </div>
</div>
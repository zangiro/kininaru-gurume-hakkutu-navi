<% breadcrumb :playlist_new, @user %>


<%= form_with model: @playlist do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  
  <%= f.label :title %>
  <%= f.text_field :title, class: "form-control" %>

  <%= f.submit nil, class: "btn btn-primary" %>
<% end %>
<div class="bg-info">
<a href="<%= post_path(post) %>" style="color: inherit; text-decoration: none;">
<%= post.title %>
<% if post.images.attached? %>
  <% post.images.each do |image| %>
    <%= image_tag image %>
  <% end %>
<% end %>
</a>
</div>